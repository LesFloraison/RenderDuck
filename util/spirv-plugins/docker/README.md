This folder creates a docker container pre-configured for compiling RiderDuck's SPIR-V plugins with the oldest possible distribution, for maximum compatibility of the binary build.

It doesn't contain anything RiderDuck-specific, apart from being tailored to its dependencies. It depends on riderduck-build from the normal build scripts.
